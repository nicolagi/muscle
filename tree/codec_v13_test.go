package tree

import "testing"

func TestCodecV13(t *testing.T) {
	t.Run("defaults for properties added in later codecs", func(t *testing.T) {
		// Placeholder.
		// * node flags (v14)
		// * block size (v14)
	})
}
